      <form hx-post="{{base_dir}}/php/login.php" method="post" hx-target="#{{target}}">
        <label for="userEmail">Email</label>
        <input type="email" name="email" id="userEmail" autocomplete="email" required />
        <label for="userPassword">Password</label>
        <input type="password" name="password" id="userPassword" autocomplete="current-password" required />
        <label for="remember">Remember</label>
        <input type="checkbox" id="remember" name="remember" />
        <div class="spacer"></div>
        <input type="submit" value="Log In" />
      </form>
      <a hx-post="{{base_dir}}/php/registerForm.php?target={{target}}" hx-target="#{{target}}">Register</a> || <a hx-post="{{base_dir}}/php/resetPW.php?target={{target}}" hx-target="#{{target}}">Reset Password</a>