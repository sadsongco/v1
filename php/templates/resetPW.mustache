<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="{{base_dir}}/styles/imports.css" />
  <link rel="stylesheet" href="{{base_dir}}/styles/main.css" />
  <script src="{{base_dir}}/lib/htmx.min.js"></script>
  <title>Unbelievable Truth</title>
</head>
<body>
  <main>
      <section class="callout">
        <div>unbelievable truth</div>
      </section>
    <section class="resetPW">
      <h2>User area password reset</h2>
      {{#resetPWForm}}
        {{>resetPWForm}}
      {{/resetPWForm}}
      {{#passwordResetError}}
        {{>PWerror}}
      {{/passwordResetError}}
      {{#passwordReset}}
        {{>PWreset}}
      {{/passwordReset}}
    </section>
  </main>
</body>