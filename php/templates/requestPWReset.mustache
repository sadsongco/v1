    <form hx-post="{{base_dir}}/php/confirmResetPW.php" method="post" hx-target="#{{target}}">
      <label for="userEmail">Email</label>
      <input type="email" name="email" id="userEmail" autocomplete="email" required />
      <div class="spacer"></div>
      <input type="submit" value="Reset Password" />
    </form>
    <a hx-post="{{base_dir}}/php/registerForm.php?target={{target}}" hx-target="#{{target}}">Register</a> ||       <a hx-post="{{base_dir}}/php/loginForm.php?target={{target}}" hx-target="#{{target}}">Log In</a> 