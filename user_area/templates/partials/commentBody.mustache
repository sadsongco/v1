<div class="commentContainer">
    <div class="username">{{username}}</div>
    <div class="articleDate">{{comment_date}}</div>
    <div class="commentBody">{{comment}}</div>
    <input type="hidden" name="comment_id" value="{{comment_id}}" />
</div>